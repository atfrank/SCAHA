#include <R.h>
#include "hungarian.h"

void hungarian(int *nin, int *x, Sint *p)
{
	n = nin[0];
  cost = x;
  int a = run();
//  printf("%d\n", a);
  int i;
  for(i = 0; i < n; i++)
	 p[i] = xy[i];
}
